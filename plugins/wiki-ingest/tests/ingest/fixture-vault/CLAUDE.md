# Vault rules (fixture)

Allowed `type` values: `concept`, `knowledge`, `how-to`, `customer`, `project`, `meeting`, `dashboard`. The list can grow.

## Status lifecycle

- `draft`: raw capture.
- `review`: enhanced by ingest.
- `evergreen`: approved.
- `archived`: in `99. Archived`.

## Rules

1. Links to [[Nowhere In Rules]] in this file are ignored.
